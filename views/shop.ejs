<%- include('partials/header', { page: 'All Products' }) %>
  <div class="container">
    <h1>Lista de produtos</h1>
    <% for (let item of products) { %>
      <li><%= item.title %></li>
    <% } %>  
  </div>
<%- include('partials/footer') %>
